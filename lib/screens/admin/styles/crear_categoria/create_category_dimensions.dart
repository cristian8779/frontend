import 'package:flutter/material.dart';

/// Dimensiones y espaciados para la pantalla de crear categoría
class CreateCategoryDimensions {
  // Alturas
  static const double imageContainerHeight = 200.0;
  static const double lottieSize = 100.0;
  static const double iconSize = 60.0;
  static const double loadingIndicatorSize = 18.0;

  // Espaciados verticales
  static const SizedBox spacingSmall = SizedBox(height: 10);
  static const SizedBox spacingMedium = SizedBox(height: 16);
  static const SizedBox spacingLarge = SizedBox(height: 30);
  static const SizedBox spacingXLarge = SizedBox(height: 40);

  // Padding
  static const EdgeInsets screenPadding = EdgeInsets.all(24);
  static const EdgeInsets cardPadding = EdgeInsets.all(20);

  // Border radius
  static const double borderRadiusSmall = 12.0;
  static const double borderRadiusMedium = 14.0;
  static const double borderRadiusLarge = 16.0;

  // Elevación
  static const double appBarElevation = 1.0;

  // Stroke width
  static const double loadingStrokeWidth = 2.0;

  // Font sizes
  static const double fontSizeNormal = 16.0;
  static const double fontSizeToast = 16.0;
}