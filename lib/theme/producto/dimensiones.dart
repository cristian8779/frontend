/// Alturas y dimensiones de componentes
class ProductoHeights {
  // Botones
  static const double buttonDesktop = 56.0;
  static const double buttonTablet = 52.0;
  static const double buttonMobile = 48.0;
  
  // Imágenes
  static const double imageDesktop = 500.0;
  static const double imageTablet = 450.0;
  static const double imageMobile = 400.0;
  
  // Selectores
  static const double colorSelectorSize = 48.0;
}

/// Duraciones de animaciones
class ProductoAnimations {
  static const Duration fadeIn = Duration(milliseconds: 800);
  static const Duration zoomTransition = Duration(milliseconds: 300);
}