import 'package:flutter/material.dart';

/// Paleta de colores para la pantalla de búsqueda
class BusquedaColors {
  BusquedaColors._();

  // Colores de fondo
  static const Color background = Color(0xFFFAFAFA);
  static const Color card = Color(0xFFFFFFFF);

  // Colores pastel
  static const Color primaryPastel = Color(0xFFE8F4FD);
  static const Color accentPastel = Color(0xFFF0F8E8);
  static const Color rosePastel = Color(0xFFFDF2F8);
  static const Color lavenderPastel = Color(0xFFF3F0FF);
  static const Color peachPastel = Color(0xFFFFF7ED);

  // Colores de acento
  static const Color primaryBlue = Color(0xFF3498DB);
  static const Color primaryGreen = Color(0xFF27AE60);
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color errorRed = Color(0xFFFF6B6B);
}